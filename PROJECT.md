# PROJECT.md — 값과 근거

행동 규칙은 `CLAUDE.md`, 용어 뜻은 `용어사전.md`. 여기는 **숫자**만 다룬다.

문턱·기준값을 새로 정하거나 바꾸면 **이 표를 같은 커밋에서 갱신한다.**
근거 없이 도는 값은 `잠정` 으로 남겨 둔다. 지우지 않는다 — 안 보이면 안 고친다.

---

## 값 → 쓰는 곳 → 근거

| 값 | 쓰는 곳 | 근거 | 상태 |
|---|---|---|---|
| 0.2초 pause | gblite | De Jong & Bosker 2013 (`papers/asr/jong13_diss.pdf`) | 확정 |
| 0.3초 gap | README | Stivers et al. 2009 (`papers/ca-canon/`) | 확정 |
| 1.0초 large pause | README | Jefferson 1989 (`papers/ca-canon/`) | 확정 |
| 0.29초 측정침묵 하한 | aligned 프리셋 | De Jong & Bosker 250~300ms | 확정 |
| 0.17초 미세침묵 하한 | aligned 프리셋 | CrisperWhisper 160ms + 20ms 격자 회피 (`papers/asr/CrisperWhisper_2024_*`) | 확정 |
| collar 0.12초 | 화자분리 채점 | Heldner 2011 | 확정 (원문 없음) |
| min_dur 0.10 | `overlap_spans` | 실측 — 0.05~0.20 에서 결과 불변 | 확정 (민감도 있음) |
| **0.50초 TURN_END** | aligned 프리셋 | **없음 — 분절 파라미터** | **잠정** |
| **2초 FTO 상한** | 분석 스크립트 | **없음 — Hoey 2015 가 반박** | **잠정** |
| **0.35 VAD** | 2.1절 결론 | 실측으로 결론 났으나 **코드는 아직 0.5** | **미반영** |
| `loudnorm I=-25 TP=-0.4 LRA=11` | wav 정규화 (`data/원본미디어_위치.md`) | 원 명령 미기록 — 기존 정규화본에서 역산, 0.6 dB 이내 재현 | 잠정 |
| ±2턴 발췌 창 | `tools/ca/twolayer.py` | 없음 — 편의값 | 잠정 |
| Yale 로마자 대응표 | `tools/ca/twolayer.py` | Martin 1992 체계, 자모 대응만 구현(형태음운 축약 없음) | 잠정 |
| 4 fps 화면 판독 | `tools/sogang/state_track.py` | 재려는 양(1~6초)의 4~24배 해상도 | 확정 |
| 버튼 색 문턱 초록·빨강 500px · 분홍 150 · 회색 1200 | 같은 파일 | 실측 — 네 상태가 2434/382/2307/1963px 로 겹치지 않음(0820 세션 5프레임) | 확정 |
| 0.5초 순간깜빡임 제거 | `tools/sogang/turns.py` | 실측 — 걸러낸 11개가 전부 길이 0.00초 | 확정 |
| 발화 끝 = RMS 문턱 150 · 100ms 지속 | 같은 파일 | 실측 — 문턱 150~600 에서 값 불변. 지속 조건 없으면 한 턴이 3.0초 늦게 잡힘 | 확정 (민감도 있음) |
| x 0.12 좌우 화자 문턱 | `tools/sogang/chat_ocr.py` | 실측 — 튜터 말풍선 왼쪽 끝이 x=0.052 고정, 학생은 오른쪽 정렬 | 확정 |

### 우리 값이 아닌데 우리 계산에 들어오는 값 (서강튜터 앱, 2026-08-21 확인)

지연 분해에서 이 값들을 빼거나 더한다. **앱이 바뀌면 우리 수치가 같이 바뀐다.**
배포본이 아래 소스와 같은지 확인하지 않았다 — `tutoring_ca_metrics` 의
`silenceWaitMs` 를 조회하면 그 자리에서 확인된다.

| 값 | 어디 | 무엇 |
|---|---|---|
| `SILENCE_DISPATCH_MS = 4000` | `useSpeechRecognition.ts:74` | 종결어미로 보일 때 침묵 대기 |
| `SILENCE_DISPATCH_UNFINISHED_MS = 7000` | `:75` | 미완결로 보일 때 (세션 D 에서는 0번 쓰임) |
| `MERGE_WINDOW_MS = 1000` | `TutoringSession.tsx:476` | 전송 뒤 조각을 더 모으는 대기 |
| `COOLOFF_MS = 0` | `useSpeechRecognition.ts:77` | TTS 뒤 재무장 대기 |

**이 표의 존재 이유는 아래 셋이다.** 0.50 과 2초가 근거 없이 돌고 있고,
0.35 는 결론이 났는데 코드에 안 들어갔다. 표가 없으면 이게 안 보인다.

### 미세침묵 하한 — 민감도 (실측, 2026-08-13)

`out_aligned_260813` 76세션, 같은 화자 낱말 간격 98,769개.
재현 검증: 하한 0.17 로 센 7,674 개가 실제 marker CSV 의 7,608 개와 0.9% 차.
재현 스크립트 `tools/micro_sweep.py`.

| 하한 | 미세침묵 개수 | 0.17 대비 | 비고 |
|---|---|---|---|
| **0.17 (현행)** | 7,674 | 기준 | |
| 0.20 | 5,663 | −26% | |
| 0.25 | 1,885 | −75% | |
| 0.29 이상 | 0 | −100% | 띠가 빈다. `LB_PAUSE` 도 같이 올려야 함 |

**분포에 0.15초 절벽이 있다.** 0.10~0.15 구간 18,911개 → 0.15~0.20 구간
4,356개로 4.3배 떨어진다. 그 앞쪽 덩어리는 조음 간격으로 보이며, 현행 0.17 은
절벽 바로 위다. 0.15~0.29 구간은 완만하므로 **개수로는 최적값을 집을 수 없다.**
이 값은 실측이 아니라 **판단**으로 정해야 하고, 그래서 교수님께 여쭐 항목이다.

### 잠정 값을 다룰 때

- **0.50초 TURN_END** — 분절 파라미터라 자유 파라미터다. 이 값으로 나온 턴 수·
  턴 길이를 근거로 쓸 때는 민감도 표를 같이 낸다.
  → 근거를 붙일 후보가 생겼다: **Heldner & Edlund 2010** (`papers/ca-canon/`) 이
  실제 대화의 pause·gap·overlap **분포**를 실측했다. 임의의 0.5 대신 그 분포의
  백분위로 잡으면 자유 파라미터가 근거 있는 값이 된다. 2초 상한도 같은 방법.
- **2초 FTO 상한** — `소강(lapse)` 이라고 부르지 않는다. Hoey 2015 는 lapse 를
  길이로 정의하지 않으며, 우리가 하는 것은 **분석상의 절단**이다.
  보고서 표현은 「2초 상한 (초과분 제외)」로 통일했다 (2026-08-13).
- **0.35 VAD** — 반영 전까지 2.1절 결론을 현행 산출물의 설명으로 쓰지 않는다.

---

## 인용 금지 상태

**모든 수치는 인용 금지다.** 사람 CA 전사본과 대조하기 전이다.
대조가 끝난 값만 이 문서에서 딱지를 뗀다.

주석자 간 일치도가 자동 검출의 천장이다 (`용어사전.md`). 사람끼리 91%면
자동 검출을 91% 위로 평가할 근거가 없다.

---

## 문헌으로 조심할 것

| 하지 말 것 | 이유 | 출처 |
|---|---|---|
| 길이 문턱으로 자른 것을 `lapse` 라 부르기 | lapse 는 길이로 정의되지 않는다 | Hoey 2015 |
| "비선호는 지연된다"를 문헌 근거로 쓰기 | 195건에서 체계적 차이 없음. 700ms 이상에서만 | Kendrick & Torreira 2015 |
| Stivers 기준선에 산포 대조하기 | Stivers 는 질문–응답 인접쌍만 재어 긴 침묵이 안 들어온다 | 개발기록 2.51절 |

---

## 논문 서고

```
papers/
  ca-canon/      16편  CA 원전 (SSJ 1974, Jefferson 1979/1983/1989/2004,
                       Schegloff 1973/1977/1992/2000, Stivers 2009, Hoey 2015,
                       Kendrick & Torreira 2015, Levinson & Torreira 2015,
                       Heldner & Edlund 2010, Hepburn & Bolden 2013,
                       Roberts & Francis 2013)
  ca-laughter/    8편  JoP 42(2010) 웃음 특집호 (Jefferson 추모)
                       + _같은호_비웃음/ 14편
  ca-applied/    10편  지도교수 계열
  asr/           25편  음성인식·정렬·화자분리·웃음검출
  _txt/                전편 텍스트 추출본 — **grep 은 여기 건다**
  INDEX.md             질문 → 논문 지도
```

**원문이 없어 인용만 하고 있는 것** (우선순위 순):

1. **Potter & Hepburn 2010**, JoP 42(6) **1543–1555** — IPA 개념. 받으신
   특집호에서 이것만 빠졌다
2. Schegloff 1993(계량화) · Stivers 2015(자동 코딩) — 방법론 방어
3. Pomerantz 1984(선호구조) · Schegloff 1982(맞장구) · Heldner 2011(collar 0.12)
